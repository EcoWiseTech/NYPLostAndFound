AWSTemplateFormatVersion: '2010-09-09'
Description: Lambda function
Parameters:
  FunctionName:
    Type: String
    Description: "Github branch name e.g. development"
  TemplateBucket:
    Type: String
    Default: ""

Resources:
  github-to-lambda-Get-Weather-Forcast-two-hour:
    Type: 'AWS::IAM::Role'
    Properties:
      RoleName: github-to-lambda-Get-Weather-Forcast-two-hour
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service:
                - lambda.amazonaws.com
            Action:
              - 'sts:AssumeRole'
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
  github-to-lambda-Get-Weather-Forcast-two-hour:
    Type: AWS::Lambda::Function
    DependsOn: github-to-lambda-Get-Weather-Forcast-two-hour
    Properties:
      FunctionName: !Ref FunctionName
      Runtime: nodejs22.x
      Role: !GetAtt github-to-lambda-Get-Weather-Forcast-two-hour.Arn
      Handler: index.handler 
      Code:
        S3Bucket: !Ref TemplateBucket
        S3Key: !Sub lambda-files/${FunctionName}.zip
      TracingConfig:
        Mode: Active