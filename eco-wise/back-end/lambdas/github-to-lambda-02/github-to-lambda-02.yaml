AWSTemplateFormatVersion: '2010-09-09'
Description: Lambda function 2
Parameters:
  FunctionName:
    Type: String
    Description: "Github branch name e.g. development"
  TemplateBucket:
    Type: String
    Default: ""

Resources:
  LambdaRole02:
    Type: 'AWS::IAM::Role'
    Properties:
      RoleName: lambda-test-role-02
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service:
                - lambda.amazonaws.com
            Action:
              - 'sts:AssumeRole'
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
  GithubToLambda02:
    Type: AWS::Lambda::Function
    DependsOn: LambdaRole02
    Properties:
      FunctionName: !Ref FunctionName
      Runtime: nodejs22.x
      Role: !GetAtt LambdaRole02.Arn
      Handler: index.handler 
      Code:
        S3Bucket: !Ref TemplateBucket
        S3Key: !Sub lambda-files/${FunctionName}.zip
      TracingConfig:
        Mode: Active